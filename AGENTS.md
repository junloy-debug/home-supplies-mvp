# 家庭日用品管理 MVP：Agent 工作合約

## 開工門檻

- 在開始製作 Plan 前，提醒負責人先向客戶確認 MVP 範圍。
- 實作前以 vault 內的 `SPEC.yaml` 為唯一驗收依據；若要求與本文件衝突，以最新確認的 `SPEC.yaml` 為準。
- 不得自行加入未被要求的功能或業務規則。任何新增範圍須先取得客戶確認並更新規格。

## 交付邊界

- 交付物為單一 `index.html`。
- 不可載入外部 CSS、JavaScript、CDN、字型或網上 library。
- 必須在 320px 闊度的手機畫面可操作。
- 使用本機 `localStorage` 保存資料；不使用後端或網絡服務。
- 功能固定使用港幣（HKD），不提供貨幣選擇。

## 客戶明確剔除的功能

以下不是遺漏，禁止當作 MVP 的隱含需求實作：

- 登入或帳號
- 跨裝置同步
- 貨幣選擇
- Push notification

## 資料模型規則

- 「發生過的事」以紀錄保存：物品名、類型（`買咗`／`用咗`）、數量、日期，以及只屬於「買咗」的金額。
- 「仲剩幾多」是推導值：同一物品的買咗數量總和減用咗數量總和。
- **不得**保存獨立結存欄位、快取結存，或以手動結存作為顯示依據。
- 低量門檻是可保存的設定，按物品保存。
- 本月使費是推導值：僅加總本曆月的「買咗」金額；「用咗」及上月紀錄不計入。
- 本月平均單價是推導值：同一物品本月「買咗」總金額除以本月「買咗」總數量；「用咗」及其他月份紀錄不計，沒有本月購買數量時顯示 `—`。
- 所有金額以 cents（整數）或等效精確方式計算及儲存；畫面固定顯示兩位小數，例如 `HK$0.30`。

## 表單行為

- 「買咗」：金額必填，否則不可提交。
- 「用咗」：金額不必填；金額欄可停用或作 0 處理，但不可阻止提交。
- 「用咗」數量若大於現有剩餘量：拒絕提交、不得寫入 `localStorage`，並顯示 `數量唔可以多過現有存貨`；此規則不論有沒有設定門檻都適用。
- 刷新或關閉再開後，紀錄及門檻必須仍可還原。
- 首次／沒有紀錄時，顯示：`未有紀錄，加第一筆啦`。
- 「清空全部」必須先要求一次確認；取消時不得刪除資料。

## 低量狀態規則

僅限**已設定門檻**的物品，按以下規則只顯示一個紅字狀態：

1. 剩餘量等於 0：`用完咗`，並優先於低量提示。
2. 剩餘量大於 0 且小於或等於門檻：`就快用完`。
3. 其他情況：不顯示紅字。

未設定門檻時不顯示低量或用完紅字。負數不再是正常可儲存狀態；應在提交超量「用咗」紀錄時按表單規則拒絕。

## 修改原則

- 改門檻只應改設定資料，並重新計算／渲染狀態；不得改寫交易紀錄、剩餘量或使費。
- 改動交易紀錄後，清單、本月使費及狀態必須全部由紀錄重新推導。
- 對範圍或邊界有疑問時，不要猜測；先標記為待確認，而非新增規則。

## 驗收與交付

- 逐條檢查 `SPEC.yaml` 的 A1–A9 及 B1；每條均須可指出畫面或計算結果作為證據。
- 特別檢查：`0.10 + 0.20` 顯示 `HK$0.30`、門檻相等、剩餘量 0、超量「用咗」被拒絕且紀錄不變、未設門檻、跨月使費、本月平均單價及清空取消。
- 完成後更新 vault 內 `PLAN.md` 的狀態與實作紀錄（如適用），但不要把未實作項目寫成已完成。
